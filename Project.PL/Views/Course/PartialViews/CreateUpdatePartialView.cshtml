@model CourseViewModel
@{
    
}

<div class="card-body p-0">
    <div class="card card-primary">
        <div class="card-body">
            <div class="form-group">
                <label asp-for="@Model.CourseId">Course ID</label>
                <input type="text" asp-for="@Model.CourseId" class="form-control">
                <span asp-validation-for="@Model.CourseId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.CrsName">Course Name</label>
                <input type="text" asp-for="@Model.CrsName" class="form-control">
                <span asp-validation-for="@Model.CrsName" class="text-danger"></span>
            </div>
          
        
          
        </div>
        <!-- /.card-body -->
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}